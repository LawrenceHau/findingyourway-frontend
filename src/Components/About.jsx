import React from 'react';
import { Link } from 'react-router-dom';

function Footer(props) {
    return (
        <div className="footer">
            <h4>This is a game create dusing React to display data from the backend and Bootstrap to stylize the displayed data, and buttons, <br></br>The Back-End uses Django to create the Data and Serializers, as well as PostgreSql for data management. </h4>
            </div>
    );
}

export default Footer;